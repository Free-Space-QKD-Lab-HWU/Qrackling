classdef (Abstract) QKD_Transmitter
    %QKD_Transmitter an abstract class implementing all of the behaviour that a
    %transmitter requires (whether satellite or OGS).

    properties
        %object containing transmitter details
        Source =[]
    end
end
